import { motion } from "framer-motion";

import { styles } from "../styles";

const HeroSolo = () => {

  

  return (
    <section className={`relative w-full h-screen mx-auto bg-inicioSolo bg-cover bg-no-repeat bg-center bg-fixed z-2`}>
      <div
        className={`absolute inset-0 top-[120px]  max-w-7xl mx-auto flex flex-row items-start gap-5`}
      >
        
      </div>
    </section>
  );
};

export default HeroSolo;
